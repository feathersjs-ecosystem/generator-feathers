{
  "host": "localhost",
  "port": 3030,
  "public": "../public/"<% if(authentication.length){ %>,
  "auth": {<% if(authentication.indexOf('local' > -1)){ %>
    "local": {
      "secret": "<%= secret %>",
      "loginEndpoint": "/login",
      "userEndpoint": "/users",
      "usernameField": "email"
    }<% } %>
  }<% } %>
}
